// all the bellow code's meaning is we want to use context api.

import { createContext } from "react"; // imported createContext from the react

const noteContext = createContext(); // created noteContext which has value of createContex() built in function to user createContext api.

export default noteContext; // exported noteContext.